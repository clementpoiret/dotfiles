---@type ChadrcConfig
local M = {}

M.ui = {
  theme = 'catppuccin',
  theme_toggle = {'catppuccin', 'github_light'},

  nvdash = {
    load_on_startup = true,
  }
}
-- M.plugins = 'plugins'
-- M.mappings = require 'mappings'

return M
